{% extends "::template.html.twig" %}
{%block body%}

      {%block container%}
	  
	  <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                           <center> <h1 style="color:blue; font-weight:bold">Consulter demandes</h1></center>
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <div class="table-responsive" style="border:blue 2px solid; width:75%; margin:auto">
                                <table width=75% class="table table-striped table-bordered table-hover"  >
                                    <thead>
                                        <tr>
                                            <th>Code</th>
                                            <th>date dépot</th>
                                            <th colspan=2 >Période:
											 De <span style="position:relative; left:90px">à</span> </th>
											
                                            <th>Type</th>
											<th colspan=2>Etat</th>
                                        </tr>
                                    </thead>
                                    <tbody>
									{%for line in liste%}
                                        <tr class="odd gradeX">
                                            <td>{{line.personnel}}</td>
                                            <td>{{line.dateDepot}}</td>
                                            <td>{{line.dateDebut|date('Y-m-d')}}</td>
                                            <td class="center">{{line.dateFin|date('Y-m-d')}}</td>
											<td class="center">{{line.type}}</td>
											<td class="center">
											<a href="{{path('cause_refuse',{'id':line.id})}}">
											<img src="{{asset('bundles/pfe/admin/images/refuser.jpg')}}" width=50 height=50/>
											</a>
											                    <a href="{{path('conge_validation',{'id':line.id})}}" style="position:relative; left:30px"><img src="{{asset('bundles/pfe/admin/images/accepter.jpg')}}" width=50 height=50/></a>
																</td>
                                           
										</tr>
                                        {%endfor%}
                                    </tbody>
                                </table>
                            </div>
	  
	  
	  
	  
	  
	  
	  {%endblock%}
  {%endblock%}   