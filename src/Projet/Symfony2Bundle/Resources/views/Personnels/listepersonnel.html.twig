{% extends "::template.html.twig" %}
{%block body%}

      {%block container%}
	  
	  <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            <center> <h1 style="color:blue; font-weight:bold">Liste Personnels</h1></center>
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <div class="table-responsive" style="width:75%; margin:auto; border:blue 2px solid;padding:25px;">
                                <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                                    <thead>
                                        <tr>
                                            <th>Nom</th>
                                            <th>Prénom</th>
                                            <th>Code</th>
											<th>Profil</th>
                                            <th>Modifier</th>
											
											 
                                        </tr>
                                    </thead>
                                    <tbody>
									{%for line in liste%}
                                        <tr class="odd gradeX">
                                            <td>{{line.nom}}</td>
                                            <td>{{line.prenom}}</td>
                                            <td>{{line.code}}</td>
                                            <td class="center">{{line.profil}}</td>
                                            <td class="center">
											<a href="{{path('personnel_update',{'id':line.id})}}">
											
											<img src="{{asset('bundles/pfe/admin/images/gerer.jpg')}}" width=50 height=50 alt="" class="imgleft" />
											</a>
											</td>
											
                                          
										</tr>
                                        {%endfor%}
                                    </tbody>
                                </table>
                            </div>
	  
	  
	  
	  
	  
	  
	  {%endblock%}
  {%endblock%}   