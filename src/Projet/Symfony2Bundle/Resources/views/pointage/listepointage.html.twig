{% extends "::template.html.twig" %}
{%block body%}

      {%block container%}
	  
	  <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                           <center> <h1 style="color:blue; font-weight:bold">Consulter listes pointage</h1></center>
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <div class="table-responsive" style="border:blue 2px solid; width:75%; margin:auto">
                                <table width=75% class="table table-striped table-bordered table-hover"  >
                                    <thead>
                                        <tr>
                                            <th>Personnel</th>
                                            <th>date Entrée</th>
                                            <th colspan=2 >date sortie </th>
											
                                           
                                        </tr>
                                    </thead>
                                    <tbody>
									{%for line in liste%}
                                        <tr class="odd gradeX">
                                            <td>{{line.code}}</td>
                                            <td>{{line.date|date('Y-m-d')}}</td>
                                            
                                           
										</tr>
                                        {%endfor%}
                                    </tbody>
                                </table>
                            </div>
	  
	  
	  
	  
	  
	  
	  {%endblock%}
  {%endblock%}   