{%extends '::template.html.twig'%}
{%block body%}
{%block container%}
	                  
					  
{#Adresse IP: {{ip}}<br>
Date de pointage: {{dt}}<br>
personne connecté : {{app.session.get('personnel').code}}
#}

{####################}
<div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                           <center> <h1 style="color:blue; font-weight:bold">Pointage Sortie</h1></center>
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <div class="table-responsive" style="border:blue 2px solid; width:50%; margin:auto;" class="container">
                                <table width=50% class="table table-striped table-bordered table-hover">
                                    <tbody>
									<form method="post" action="{{path('pointer')}}">
                                        
										
										<tr class="odd gradeX">
                                           <td align=center><span style="font-size:18px;color:blue">{{form_label(form.date)}}</span></td>
<td>{{form_widget(form.date,{'attr': {'value': dt}})}}</td>
                                        </tr>
										<tr class="odd gradeX">
                                           <td align=center><span style="font-size:18px;color:blue">{{form_label(form.code)}}</span></td>
<td>{{form_widget(form.code,{'attr': {'value': app.session.get('personnel').code}})}}</td>
                                        </tr>
										<tr>
										<td>{{form_widget(form.pointage,{'attr': {'value': 'sortie'}})}}</td></tr>
										<tr>
										
										<td colspan=2>
                                <span class="input-group-btn" style="float:right" >
                      <button style="float:right; position:relative;top:12px" type="submit" class="btn btn-info btn-flat">Pointer!</button>
                    </span>
					</td></tr>
					
                                 </form>      
                                    </tbody>
									</table>
                            </div>
{%endblock%}
{%endblock%}